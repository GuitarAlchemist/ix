{
  "note": "Tests that can return before reaching an assertion when their environment is missing, and so report a pass having checked nothing. Checked by `ix doctor` (see crates/ix-skill/src/doctor/silent_skips.rs). Entries are keyed by file and test name, not line. Every entry needs a non-empty `reason`: the point of this file is that a human said why a test may pass without asserting. `kind` is `environment` (the skip covers something CI cannot reasonably provide: a sibling repo's build, a gitignored artifact, a local binary; expected to stay) or `tracked` (nothing environmental stops it being checked; requires an `issue` and counts as outstanding debt). Entries whose test no longer skips are reported as stale and must be removed. This list is a baseline of what was already true when the check landed; it is not a place to park new skips.",
  "allow": [
    {
      "file": "crates/ix-agent/tests/autonomous_render_audit.rs",
      "test": "harness_discovers_moon_dark_face_issue_and_fixes_it",
      "kind": "environment",
      "reason": "Cross-repo test over two GA frontend sources, located by absolute paths under C:/Users/spare/source/repos/ga. It cannot run on CI, and it cannot run for any other contributor even with ga cloned beside ix, because the paths name one developer's home directory. Skips 9 assertions. The hardcoded path is a defect in its own right; resolving ga as a sibling of the workspace root would at least let it run locally for everyone.",
      "issue": null
    },
    {
      "file": "crates/ix-agent/tests/voicings_payload_contract.rs",
      "test": "corpus_lazy_lookup_resolves_when_present",
      "kind": "environment",
      "reason": "Guards on state/voicings/bass-corpus.json, which is committed to git, so in any clean checkout the guard never fires and the test does assert. Baselined because the scanner cannot tell a dead guard from a live one; the honest fix is an `expect` on the read, after which this entry goes stale.",
      "issue": null
    },
    {
      "file": "crates/ix-agent/tests/voicings_payload_contract.rs",
      "test": "binary_buffer_layout_matches_meta_when_present",
      "kind": "environment",
      "reason": "Needs state/viz/voicing-positions.{bin,meta.json}, which are gitignored (.gitignore: state/viz/) and produced by running serve_viz over OPTIC-K data. Never present on CI or in a fresh clone.",
      "issue": null
    },
    {
      "file": "crates/ix-duck/tests/ixql_plan_golden.rs",
      "test": "duckdb_cli_reproduces_the_frozen_golden_when_duckdb_is_installed",
      "kind": "environment",
      "reason": "Needs the `duckdb` CLI. Hosted CI runs this test for real in the `duckdb-sql` job of .github/workflows/ci.yml, which installs a pinned, sha256-checked DuckDB v1.5.3 and sets IX_REQUIRE_DUCKDB=1, so an unreachable CLI fails there instead of skipping (ix#294, #319). The bare `return;` remains for local machines without duckdb, where the test prints that the SQL surface was NOT checked.",
      "issue": "ix#294"
    },
    {
      "file": "crates/ix-duck/tests/ixql_plan_golden.rs",
      "test": "duckdb_refuses_a_corrupted_plan_when_duckdb_is_installed",
      "kind": "environment",
      "reason": "Needs the `duckdb` CLI. Hosted CI runs this test for real in the `duckdb-sql` job of .github/workflows/ci.yml, which installs a pinned, sha256-checked DuckDB v1.5.3 and sets IX_REQUIRE_DUCKDB=1, so an unreachable CLI fails there instead of skipping (ix#294, #319). The bare `return;` remains for local machines without duckdb, where the test prints that the SQL surface was NOT checked.",
      "issue": "ix#294"
    },
    {
      "file": "crates/ix-duck/tests/pareto_golden.rs",
      "test": "duckdb_cli_reproduces_the_frozen_golden_when_duckdb_is_installed",
      "kind": "environment",
      "reason": "Needs the `duckdb` CLI. Hosted CI runs this test for real in the `duckdb-sql` job of .github/workflows/ci.yml, which installs a pinned, sha256-checked DuckDB v1.5.3 and sets IX_REQUIRE_DUCKDB=1, so an unreachable CLI fails there instead of skipping (ix#294, #319). The bare `return;` remains for local machines without duckdb, where the test prints that the SQL surface was NOT checked.",
      "issue": "ix#294"
    },
    {
      "file": "crates/ix-duck/tests/persistence_h0_golden.rs",
      "test": "duckdb_cli_reproduces_the_frozen_golden_when_duckdb_is_installed",
      "kind": "environment",
      "reason": "Needs the `duckdb` CLI. Hosted CI runs this test for real in the `duckdb-sql` job of .github/workflows/ci.yml, which installs a pinned, sha256-checked DuckDB v1.5.3 and sets IX_REQUIRE_DUCKDB=1, so an unreachable CLI fails there instead of skipping (ix#294, #319). The bare `return;` remains for local machines without duckdb, where the test prints that the SQL surface was NOT checked.",
      "issue": "ix#294"
    },
    {
      "file": "crates/ix-duck/tests/persistence_h0_golden.rs",
      "test": "the_sql_guard_rejects_bad_input_when_duckdb_is_installed",
      "kind": "environment",
      "reason": "Needs the `duckdb` CLI. Hosted CI runs this test for real in the `duckdb-sql` job of .github/workflows/ci.yml, which installs a pinned, sha256-checked DuckDB v1.5.3 and sets IX_REQUIRE_DUCKDB=1, so an unreachable CLI fails there instead of skipping (ix#294, #319). The bare `return;` remains for local machines without duckdb, where the test prints that the SQL surface was NOT checked.",
      "issue": "ix#294"
    },
    {
      "file": "crates/ix-governance/src/feedback.rs",
      "test": "test_read_beliefs_from_demerzel_state",
      "kind": "environment",
      "reason": "Reads governance/demerzel/state from the Demerzel submodule. Both ci.yml jobs check out submodules recursively, so CI runs its five assertions; it skips only when the submodule is not initialised. Until 2026-09-13 it read an absolute path under one developer's home directory and returned before every assertion on every CI runner."
    },
    {
      "file": "crates/ix-optick-sae/tests/python_contract_tests.rs",
      "test": "python_contract_suites_pass",
      "kind": "environment",
      "reason": "Not an absence-skip: a missing interpreter panics. It returns only when IX_SKIP_PYTHON_CONTRACT_TESTS is set explicitly, an operator opt-out for a machine with no Python. No workflow sets it, so CI runs the suites.",
      "issue": null
    },
    {
      "file": "crates/ix-optick/tests/conformance.rs",
      "test": "conformance_open_real_index",
      "kind": "environment",
      "reason": "Needs OPTICK_INDEX_PATH pointing at a real OPTK v4 index written by GA's OptickIndexWriter: a ~175 MB gitignored artifact rebuilt from the ga repo, never available on hosted CI. All four conformance tests share this gate.",
      "issue": null
    },
    {
      "file": "crates/ix-optick/tests/conformance.rs",
      "test": "conformance_search_per_instrument",
      "kind": "environment",
      "reason": "Same OPTICK_INDEX_PATH gate as conformance_open_real_index.",
      "issue": null
    },
    {
      "file": "crates/ix-optick/tests/conformance.rs",
      "test": "conformance_instrument_counts_sum_to_total",
      "kind": "environment",
      "reason": "Same OPTICK_INDEX_PATH gate as conformance_open_real_index.",
      "issue": null
    },
    {
      "file": "crates/ix-optick/tests/conformance.rs",
      "test": "conformance_metadata_random_access",
      "kind": "environment",
      "reason": "Same OPTICK_INDEX_PATH gate as conformance_open_real_index.",
      "issue": null
    },
    {
      "file": "crates/ix-registry-check/tests/compatibility.rs",
      "test": "compare_handles_the_real_capability_registry",
      "kind": "environment",
      "reason": "Skips only when the governance/demerzel submodule is not checked out. Both ci.yml jobs that check out code use `submodules: recursive`, so CI runs it; the skip covers a non-recursive local clone.",
      "issue": null
    },
    {
      "file": "crates/ix-sentrux-annotations/tests/end_to_end.rs",
      "test": "live_sentrux_check_against_ix_worktree",
      "kind": "environment",
      "reason": "Needs a local sentrux binary at DEFAULT_SENTRUX_EXE, which is the absolute path C:/Users/spare/bin/sentrux.exe (crates/ix-sentrux-annotations/src/lib.rs). Also skips on a live transport error; the fixture tests in the same file cover parsing. The hardcoded home-directory default is used by production code (src/bin/bridge.rs, src/mcp_bridge.rs), not only this test, and deserves its own issue.",
      "issue": null
    },
    {
      "file": "crates/ix-skill/src/verbs/compile.rs",
      "test": "coverage_baseline_tfidf_over_probe_corpus",
      "kind": "environment",
      "reason": "Reads state/thinking-machine/coverage-probes.jsonl, which is committed, so CI runs it. It skips only when the crate is tested outside the workspace tree (Codex #79 P2)."
    },
    {
      "file": "crates/ix-skill/src/verbs/compile.rs",
      "test": "embedding_sweep_english_models_over_probe_corpus",
      "kind": "environment",
      "reason": "Behind the `embeddings` feature, which no CI job enables, so this test is never compiled there; state/registry/dark-features.allow.json already tracks that. When enabled, the guard fires only outside the workspace tree, since the probe corpus is committed; the real dependency is the model download."
    },
    {
      "file": "crates/ix-skill/src/verbs/compile.rs",
      "test": "ood_scoring_method_sweep_over_probe_corpus",
      "kind": "environment",
      "reason": "Behind the `embeddings` feature, which no CI job enables, so this test is never compiled there; state/registry/dark-features.allow.json already tracks that. When enabled, the guard fires only outside the workspace tree, since the probe corpus is committed; the real dependency is the model download."
    },
    {
      "file": "crates/ix-skill/src/verbs/embed_coverage.rs",
      "test": "embedding_ranks_in_domain_above_out_of_domain",
      "kind": "environment",
      "reason": "Behind the `embeddings` feature, which no CI job enables, so this test is never compiled there; state/registry/dark-features.allow.json already tracks that. When enabled, it skips when bge-base-en cannot be loaded (offline, or no ONNX runtime), which a hosted runner without a model cache cannot provide."
    },
    {
      "file": "crates/ix-skill/src/verbs/pipeline.rs",
      "test": "default_constitution_matches_committed_pin",
      "kind": "environment",
      "reason": "Reads the default constitution from the Demerzel submodule and the committed pin in state/governance/constitution-pins.json. CI checks out submodules recursively, so it runs there; it skips only outside the workspace or without the submodule."
    },
    {
      "file": "crates/ix-skill/tests/local_embed_cli.rs",
      "test": "local_embed_emits_vectors_from_a_preseeded_cache",
      "kind": "environment",
      "reason": "Needs IX_TEST_FASTEMBED_CACHE pointing at a pre-downloaded fastembed model cache. CI never downloads ONNX models (see the `embeddings` entry in dark-features.allow.json). Skips 7 assertions.",
      "issue": null
    },
    {
      "file": "crates/ix-voicings/src/lib.rs",
      "test": "grammar_file_parses",
      "kind": "environment",
      "reason": "find_grammar_file looks for grammars/progressions.cfg relative to the working directory. cargo test runs a crate's unit tests from the crate directory, where that file is committed, so CI runs it; it skips only when the test binary is started from another directory."
    },
    {
      "file": "crates/ix-voicings/tests/smoke_phase_a.rs",
      "test": "smoke_phase_a_guitar_50",
      "kind": "environment",
      "reason": "Shells out to the GA FretboardVoicingsCLI build (IX_VOICINGS_GA_CLI, or the sibling ga repo's Debug output). A .NET build from another repository; never present on CI. Skips 10 assertions, the most of any baselined test.",
      "issue": null
    }
  ]
}
