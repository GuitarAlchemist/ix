[package]
name = "ix-registrar"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
categories = ["data-structures"]
keywords = ["federation", "registrar", "catalog", "drift", "governance"]
description = "The federation/registrar deep module: turn per-repo sources into a federated catalog.jsonl and gate it for drift. Payload-agnostic over a small Record trait; ix-streeling and ix-value are its two adapters."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
